# Live Traffic Floating Widget

A lightweight desktop floating ticker written in Python (Tkinter) to track commute traffic time like a live score card.

## How to run
1. Install dependencies: `pip install -r requirements.txt`
2. Run script: `python app.py`
3. Left-click & drag to reposition. Right-click to close.
